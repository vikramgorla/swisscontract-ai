# ── SwissContract Environment ─────────────────────────────────────
# Infomaniak AI (Swiss-sovereign)
INFOMANIAK_AI_TOKEN=your_infomaniak_ai_token_here

# App config — set to "production" for production, anything else for non-prod
# Injected at deploy time via Docker -e flag; do NOT set in .env.production
NEXT_PUBLIC_ENV=development
